# 1. 🌍 角色与项目介绍 (Meta-Context)
- **角色定义**: Senior Backend Architect (遵守 TDD 与 DDD 原则)
- **项目介绍**: 本项目是一个用于自动生成"良率日报"的系统。系统主要涉及数据提取、数据分析、语句输出三项核心功能。由于业务规则频繁变动，核心的分析模块（Core 层）将重度依赖大模型（DeepSeek 或 Gemini）进行动态推理和分析处理，最终输出标准化、定制化的 Excel 报告。

# 2. 📂 渐进式披露 (Progressive Disclosure)
- **词汇表 (Glossary)**:
  - **CT异常**: 发生于 CT 站点并已被识别的异常，记录于《CT异常管理表》中。
  - **批次恶化**: 指最新批次的不良率/良损高于前三个批次的最大值。
  - **集中过货**: 特定的良率波动影响规则（具体规则将在开发对应模块时明确）。
  - **Snapshot**: Parquet 格式的本地磁盘快照，用于缓存加速与断网降级。
  - **TTL**: Time-To-Live，缓存有效期，如快照 TTL=8h。

- **🚫 负面清单 (Negative Constraints)**:
  - **模型限制**: 大模型 API 仅允许使用 DeepSeek 或 Gemini，绝对禁止引入其他第三方模型 API。
  - **模块注释规范**: Core 层的每个程序文件最上方必须列出该程序详细的解释，如果调用了大模型，**必须显式列出使用的完整 prompt**。
  - **禁止修改数据库连接单例模式**: 采用 `__new__` 单例模式并内置失败重试机制，任何改变实例化方式的修改都会破坏 `.env` 延迟加载和断线重连的容灾能力。
  - **禁止消除缓存装饰器**: 服务中大量使用（如 `@st.cache_data` 等）进行 L2 缓存，移除或变更这些装饰器将导致频繁触发全量数据库查询和重计算，造成性能灾难。
  - **禁止修改 Parquet 快照的增量更新逻辑**: 增量更新模式（TTL 保护 + 缓冲窗口 + 三防线容灾降级）经过精心设计，任何简化都可能导致数据不一致或数据库过载。

- **细项规则映射**:
  - **业务核心框架 (来源于业务架构图)**:
    1. **数据获取阶段**: 
       - 需整合《V3良率及不良率By月周天汇总报表》(用于Gap计算)、《V3良率及不良率By批次汇总报表》(判断最新产出率>30%的批次是否恶化)、《CT异常管理表》(搜索当日与已知异常)、《良率目标拆解表》(获取基准目标) 以及《日良率Gap分析模板》等至少5份源表。
    2. **数据分析阶段 (By产品维度进行)**:
       - **Gap分析**: 
         - 计算日良率Gap：锁定当日Group不良率与目标不良率，算出 Top3 Gap。
         - 批次良率分析：判断是否批次恶化。若恶化，则找出对应批次及Group下影响最大的 Code 不良；若未恶化，则判定为"集中过货影响"。
         - 输出为固定句式。
       - **异常分析**: 
         - 识别"新增异常"与"影响当日良率的已知异常"（每个 Group 下的 Top3 Code）。
         - 输出时进行回复精简，并使用 html 代码做颜色标识。
       - **趋势分析**: 
         - 侦测连续三日或三周的良率下降/上升趋势，并输出固定句式。
    3. **报告输出阶段**:
       - 汇总并生成 Excel，输出三大核心模块：【当日Gap解释】、【当日异常】、【已知异常】。

# 3. 🏗️ 系统架构约定 (Architecture Blueprint)
- **项目结构 (DDD)**:

```
project_root/
├── .env                          # 环境变量（数据库凭证、DeepSeek/Gemini API Key）
├── pyproject.toml                # 项目元数据 & uv 依赖声明
├── uv.lock                       # 依赖锁定文件
├── app/                          # [展示层/Presentation] 前端应用或 API 路由（用于文件上传和报表下载）
│   ├── utils/                    #   前端/应用工具
│   │   ├── app_setup.py          #     应用初始化（日志+环境变量）
│   │   ├── logger_setup.py       #     [企业级日志架构] 按领域+级别二维隔离
│   │   └── reloader.py           #     [代码热重载] 模块卸载 + 项目指纹
├── config/                       # [配置层] YAML 配置仓库
│   ├── global.yaml               #   全局配置
│   └── products/                 #   业务单元/产品级配置（各产品的特定规则、LLM prompts 配置等）
├── src/                          # [领域层/Domain] 核心业务逻辑
│   ├── shared_kernel/            # [共享内核] 通用基础设施
│   │   ├── config.py             #   ConfigLoader: 配置工厂（YAML加载+深度合并+Pydantic校验）
│   │   ├── config_model.py       #   AppConfig: Pydantic V2 配置模型
│   │   ├── infrastructure/
│   │   │   └── llm_handler.py    #   [新增] LLMManager: DeepSeek/Gemini API 调用单例与重试机制
│   │   └── utils/
│   ├── yield_report/             # [良率报告领域]
│   │   ├── application/          #   应用服务层 (协调上传、提取、分析、生成的编排)
│   │   ├── core/                 #   核心领域层 (大模型重度参与的分析逻辑：Gap分析、异常分析、趋势分析。顶部需写明 prompt)
│   │   └── infrastructure/       #   基础设施层 (文件 IO 处理, Excel 数据提取, Prompt 模板加载)
├── resources/                    # [资源层] 静态文件与基线数据 (上传的各维度报表缓存区)
├── tests/                        # [测试层]
│   ├── conftest.py               #   Pytest Fixtures
│   ├── unit/                     #   单元测试
│   └── integration/              #   集成测试
└── logs/                         # [运行时日志] 按天轮转，自动清理
```

---

- **基础工程规范 (Core Infrastructure Patterns)**:
  - **技术栈选型**: UI 层采用 `Streamlit` 构建；包管理使用 `uv`；配置与结构化输出依靠 `Pydantic V2`；大模型交互直接使用原生 SDK (`openai` / `google-genai`，确保仅使用 DeepSeek / Gemini) 结合结构化指令（不引入庞大的 Agent 框架以保持轻量）；Excel 处理采用 `pandas` 与 `openpyxl`。
  - **依赖管理**: 项目使用 `uv` 作为包管理工具。依赖声明在 `pyproject.toml`，开发依赖在 `[tool.uv] dev-dependencies`。虚拟环境路径配置在 `[tool.pyright]` 的 `venv=".venv"`。
  - **配置管理 (Config)**: 基于 **Pydantic V2** 的链式配置体系。静态配置工厂调用链为：加载 `.env` → 加载 `global.yaml` → 加载子配置 `xxx.yaml` → 深度合并 → Pydantic 校验。单例模式隐式实现，支持链式访问。
  - **安全与凭证**: LLM API Key (DeepSeek/Gemini) 和其他敏感数据统一存储于项目根目录的 `.env` 文件，被 `.gitignore` 排除。通过 `load_dotenv` 加载为进程环境变量。
  - **日志管理 (Logging)**: 采用 **用途 × 领域 二维隔离** 策略：
    - **领域分流**（纵轴）: 根据代码文件路径自动将日志分流到对应领域的 `.log` 文件。
    - **级别隔离**（横轴）: 区分全量流水、高优报警（如 ERROR）、独立调试追踪（TRACE）。
    - 采用按天午夜零点自动轮转，过期自动清理。
  - **数据刷新机制**: 三层失效刷新体系：
    1. **L1 (Snaphost 快照)** - 维护 Parquet 格式本地快照文件，带 TTL 超时控制，支持增量更新缓冲窗口与强制全量刷新；
    2. **L2 (内存缓存)** - 服务层方法使用内存缓存装饰器，通过签名（如文件mtime+size哈希）实现自动失效；
    3. **代码刷新** - 强制卸载受控模块机制以支持热重载，并基于项目指纹(MD5)使缓存失效。

# 4. 🎯 业务需求与边界 (Scope & Boundaries)
- **IN Scope (已实现边界)**: 
  - 通过前端（暂定本地或 Streamlit）手动上传多源报表数据。
  - 基于大模型的智能规则分析引擎（日良率Gap分析、批次恶化判断、CT异常解析、连续三日/三周趋势分析）。
  - 对提取和推理后的数据进行格式化、颜色标识（HTML 代码）生成。
  - 最终合成固定的 Excel 格式报告（含当日Gap解释、当日异常、已知异常等固定句式输出）。
- **OUT of Scope (未实现/纯规划)**: 
  - 从内部共享盘/邮件系统的自动报表爬取与下载（将来规划单独模块，当前版本纯上传机制）。

# 5. 🤖 开发规范：多 Agent 协作 (EPCC Flow)
1. **Explore**: 必须先阅读相关文件，读懂上下文后再行动。
2. **Plan**: 必须先输出修改计划，交由人类审核。
3. **Code**:
   - **开发顺序约束**: 采用渐进式开发。必须**先编写基础模块（Shared Kernel/Infrastructure）和界面脚手架（UI/API），保证测试链路畅通后，再集中实现业务核心分析模块（Core 层的 LLM Prompt 交互规则）**。
   - **防御性编程**: 必须包含 Type Hints 和基础异常捕获。
   - **🚨 熔断机制**: 同一个 Bug 连续修复 5 次失败，必须立即停止并要求人类介入。
4. **Commit (TDD)**:
   - 必须先写测试。
   - **✅ 验收标准**: `uv run pytest tests/ -v --tb=short`，必须达到 100% PASS 才算完成。

# 6. 💻 系统环境与命令 (System Commands)
- **运行命令**: [待确定，如 `uv run streamlit run app/app.py`]
- **测试命令**:
  ```bash
  uv run pytest tests/ -v --tb=short
