# Project Config Index

## Folder Routes

No child folders yet.

## Read Guidance

Use this area for non-secret configuration explanations, environment matrices, dependency policy notes, and setup assumptions. Do not store `.env`, credentials, cookies, or portal session material here.

## Commands

Run `uv sync` after dependency changes, `uv run pyright` after typed config changes, and focused tests for config consumers.

## Update Rule

Add folders here for configuration explanations, environment matrices, and non-secret setup notes.
