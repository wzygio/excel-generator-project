[project]
name = "yield-report-generator"
version = "0.1.0"
description = "自动生成良率日报的系统 - 基于LLM的智能分析引擎"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
    "openai>=1.0.0",          # DeepSeek 兼容 OpenAI SDK
    "google-genai>=1.0.0",    # Gemini SDK
    "pyarrow>=10.0.0",
    "tenacity>=8.0.0",        # 重试机制
    "requests>=2.33.1",
    "beautifulsoup4>=4.14.3",
    "pywin32>=311",
    "playwright>=1.59.0",
    "xlsxwriter>=3.2.9",
    "fr-web-automation",
    "socksio>=1.0.0",
    "letta-client>=1.12.1",
    "langgraph>=0.2.0",
    "pydantic-ai-slim[openai]>=2.3.0",
    "streamlit>=1.37.0",
    "sqlalchemy>=2.0.0",
    "psycopg2-binary>=2.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
    "pyright>=1.1.410",
]

[tool.uv]
dev-dependencies = ["yield-report-generator[dev]"]

[tool.hatch.build.targets.wheel]
packages = ["src/yield_report", "src/shared_kernel"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]
ignore = ["E501"]


[tool.uv.sources]
fr-web-automation = { path = "D:/wzy/Python/packages/web_automation", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
