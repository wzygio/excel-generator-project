【Background】
为了从一个特殊的系统中下载报表，我们需要开啊一个专门的skills。

【Task1】
谢谢。是的，我们必须升级这个skill，按照如下模式：

1. 让其优先复用fr_web_automation。
2. 如果fr_web_automation不能满足，再按照fr_web_automation中的模式与架构编写Playwright代码，或调用Playwright MCP。
3. 但无论是哪种方式，成功之后（是否成功需经用户确认）都要将代码合并到fr_web_automation中，后续继续复用fr_web_automation中的代码。

也就是说，fr_web_automation是该skill的核心，这是因为操控fr（FineReport-帆软报表系统）必须要足够快，现写代码不仅token消耗大，而且无法满足对于速度的要求。

-------------

【Task2】
谢谢。但是不要将web_automation 架构审查结论写入skill 的 Architecture Notes中，这不是skill该了解的东西。

【Goal】
1. 请你将该部分内容写入web_automation包下的一个md文件中。
2. 将skill中的该部分内容替换为一个可以迭代优化web_automation的功能：“当新增代码时，请先审查原始包的架构，如果有冲突请判定应该修改代码，还是优化原始包架构，并将结论反馈给用户”（大致意思如上，您可以优化这段prompt）。
3. 请你确保该skill存在预扫描机制，使其可以了解web_auotomaition的架构并正确调用（因为web_auotomaition可能会迭代优化）。比如扫描web_automation下的md文件，或者接口程序。

-------------

【Test1】
请你先尝试完成以下测试：
1. 打开帆软报表网址
2. 在搜索框中输入“V3良率及不良率By月周天汇总报表”并点击相应报表。
3. 等待报表加载完成后，在产品型号中选择“M678”（模拟“输入M678后再按下Tab”这种方式）
4. 点击查询并等待查询结果加载完成。
5. 完成报表下载。

【Context】
1. 帆软报表地址：http://10.73.17.76:8080/webroot/decision
2. 账密等必要信息位于项目根录目下的“.env”文件中，特别是以下三者：
FINEREPORT_HOST
FINEREPORT_USERNAME
FINEREPORT_PASSWORD

【Hint】我当时让AI帮助我开发这个包的方法是：通过browser中的开发者工具选中目标组件后，将其中的div信息传输给了AI。所以如果你遇到了无法解决的问题，请不要死磕，直接向我求助。因为帆软系统非常特殊，你可能根本想不到它的操作模式。

【迭代机制】
预扫描接口 → 调用接口执行任务 → 优化或新增程序 → 架构冲突判定 → 反馈用户决策 → 内容核入

-------------

【Test1-fix】
您的测试没有成功，下载的文件中没有任何数据。我觉得可能的原因有两个：
1. 由于某种原因，你没有成功输入产品型号（比如你可能没有找到对应的输入框）
2. 你在点击查询后，没有等待数据加载完成就点击了下载。

【Goal】
请重新执行测试，这次，请在你认为测试完成后不要关闭浏览器，我会查询其最终状态来发现问题。

-------------

【Test2】
上次测试成功，你的结果是正确的。没有数据的原因在于结束日期设置的太晚。
请重新执行测试：这次除了设置产品型号为“M678”之外，请将结束日期设置为“2026-05-01”（我建议采用直接输入的方式，因为日期选择器较为复杂）。

-------------

【Test3】
请继续测试：
1. 同名报表有两个（如图所示），你刚才选择的都是上方那个，但正确的是下方那个。正确的方式是通过“目录”来分辨，正确的报表位置：“目录/良率监控/综合良率”。我记得我开发的web_automation包中有处理这一细节的功能
2. 请顺带测试多产品输入：“M678”、“M626”、“M673”.（每个产品输入完成后都要按下tab）
3. 其它条件与此前一致。

-------------

【Test3-fix】
测试失败。你依旧只输入了“M678”一款产品型号。
1. 请继续尝试，这次请停留在输入产品型号这一步，不再继续，我来检查发生了什么问题。
2. 此外，请尝试分别查询这三款产品并下载。只不过这种方式需要你想办法重命名下载文件的名称，添加产品型号作为后缀。否则后续流程无法分别。
