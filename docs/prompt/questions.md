【Task】
你是一名资深的数学工程师，我需要您给我一个数学建议：如何动态制定规格线。

【Background】
1. 我是一家OLED制造厂的员工，我们想让智能体来代替人去检测机台生产情况。
监控指标：RS Density。每天的RS Density大小可近似视为独立事件。
时间区间：近15天
2. 我们想监控机台的RS Density是否恶化，因此需要制定规格线。但（机台数 × RSCode数）是一个百万级的数字，不可能人为制定规格，所以想根据实际数据动态制定规格线。

【Context】
我的想法是参考CPK的公式，在确定没有LSL的情况下，USL为：
USL = μ + CPK*3σ = μ + mσ
μ：均值
σ：标准差
m：人工可调节参数

【Goal】
请分析我的需求，并给出建议

----------Question 1

您的分析是正确的，RS应该符合泊松分布而不是高斯分布。目前方案A看起来不错，但还是有一些问题：

【Context】
1. 方法A：请问MAD的具体计算公式是什么，它的实际含义是什么（比如他如何衡量离散程度）
2. 方法B：不太可靠，因为数据源只有近15天的数据、
3. 方法C：不要引入PM时间这种新数据，会加大复杂度。你所拥有的只有每个机台近15天的在每个RSCode上的Density数据。

----------Question 2

您的顾虑非常正确，机台的RS数据确实经常出现0的情况（没有过货）。不过我们能否排除掉0的数据点再去计算MAD和USL，而不是设置一个最小容忍度？
因为我觉得它的本质还是规格，而不同的机台规格是不一样的。

----------Question 3

谢谢。但我还有一些建议：
1. 无需担心所谓“真实为0”的情况。我之前没有告诉您,我们实际用于计算的数据是“过货量 * RS Density”，这种情况下如果为0，那么只有两种可能：
a. 过货量为0。
b. 过货量极小，RS Density为0。
这两种情况恰恰都应该排除。
2. 不过您提出的新的问题：“样本量校验”确实需要考虑。N＜5的情况确实经常出现，这种情况下如何计算USL？
首先排除您提出的“工厂全局历史平均 USL”，根本没有也无法计算出这样一个值。

鉴于这是一个工业公式，我建议不要太过复杂：
1. 首先排出0值。
2. 不要引入“自适应容忍度”（因为现在已经没有0干扰了）。直接就是USL = Median + 1.4826 * MAD。
3. 现在唯一需要解决的问题是：当样本量较少时如何解决？或者说，当N大于多少时，“USL = Median + 1.4826 * MAD”这一公式才会有效？小于这个界限时如何处理？

----------Question 4

我的修改建议：
1. “1”这个数字根本就不可能给的出来，您必须记住一个前提：我们没有任何依赖先验知识给出的指标，只能依赖近十五天数据，否则我就不会请您帮助我制定动态规格了。
2. 为此，我认为我们应该引回“自适应容忍度”。
我们依旧使用这个公式：$$\text{USL} = \text{Median} + m \times \max(\text{MAD} \times 1.4826, \epsilon)$$
只不过 $\epsilon$ 按照如下公式计算即可：$\epsilon = 0.05 * Median$

最后一个顾虑：RS可能不是泊松分布而是高斯分布，请针对这种情况，给我一个与上述公式对应的公式：

当N=1时：$$\text{USL} = X_1 + m \times (0.05 \times X_1)$$
当1＜N＜5时：$$\text{USL} = \text{Mean} + m \times \max(S, \, 0.05 \times \text{Mean})$$
当N＞5时：$$\text{USL} = \text{Median} + m \times \max(\text{MAD} \times 1.4826, \epsilon)$$

$$\epsilon = 0.05 * Median$$
