【Background】
由于我的公司有文件加密系统，我们想让程序或智能体读取文件时，需要先将文件解密。主要包括两个部分：文件解密和内容补全。
当前已有一个skill名称，名叫“read-encrypted-xlsx”，但其需要完善。

【Introduction】
1. 文件解密：读取文件时，如果发现文件加密（一般都会加密），请调用skill将文件解密到当前项目中的“resources/decrypted_files”路径下。如果没有项目则输出到
2. 内容补全：解密后的文件一般表格结构会被破坏，尤其是合并单元格。请思考补全策略（比如向下补全和向右补全）

【Requirements】
该skill应该当具备以下特性：
1. 以程序为核心：因为解密流程重复度高，为了运行速度和token下好，每次解密要已有解密包，而非重新编写解密程序。
2. 迭代机制：当发现已有方法无法解密的新文件类型时，在探索成功之后，将新方法核入脚本中。
3. 解释文档：编写接口文档和架构文档，便于智能体调用和迭代优化。
具体形式可参考：“D:\wzy\Python\packages\web_automation”

------------
【Task1】
请读取“read-encrypted-xlsx”skill并进行完善：
1. 请编写解密程序包，并验证是否能够解密文档。可以尝试以下文件：“D:\wzy\Python\excel-generator-project\resources\V3良率及不良率By月周天汇总报表.xlsx”
2. 实现以上功能后，请制定内容补全策略并开发相应功能。并测试智能体是否能够理解补全后的文件，如果不能请优化策略。如果可以请让用户验证它的补全策略是否正确。
3. 实现以上两项功能后，请编写文档内容。将迭代机制写入skill中，并在解密程序包中编写相应的docs文件供skill调用和迭代。
