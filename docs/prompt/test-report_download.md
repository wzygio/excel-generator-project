【Background】
我要制作一个日报自动生成的项目。本项目分为三个模块：报表下载、数据分析、日报生成。

【Goal】
首先，请你帮助我完成“报表下载”模块的开发，并完成相应的任务测试。

【Introduction】
这一功能可大致分为三部分：
1. 理解并分析用户需求：重点在于提取出重要参数，包括但不限于“报表名称”、“筛选条件（可能包含多个）”。
2. 访问“帆软报表系统”，完成报表搜索、筛选条件设置、点击查询按钮。
3. 等待结果出现后，下载报表到本地路径：“D:\wzy\Python\excel-generator-project\resources”。

【Hint】
难点在于，传统的浏览器操作方式都无法成功，你必须尝试使用“Finereport Rpa”这个 skill。

【Info】
1. 帆软报表地址：http://10.73.17.76:8080/webroot/decision
2. 账密等必要信息位于项目根目录下的“.env”文件中，特别是以下三者：

FINEREPORT_HOST
FINEREPORT_USERNAME
FINEREPORT_PASSWORD

--------------------
【Task1】
1. 请你理解并分析“src\yield_report”当前架构，判断其是否合理，是否需要优化。

--------------------
【Task2】
1. 请测试：“我想要查询M678这款产品近两个月的良率”。请将这句话注入当前项目对应的接口，并测试结果（仅截止报表下载部分）。而不要自行理解并调用 Finereport Rpa 执行。
2. 你可以在此基础上再编写两个测试用例。产品型号可以选择 M626 和 M673。
3. 如果遇到问题，请进行修改。如果判断问题较为复杂，可以向我征求意见。

--------------------
【Task3】
1. 请优化当前模块对应的 UI：将选项卡保留为三个，分别对应“报表下载、数据分析、日报生成”。
2. 请重构 app 架构，可调用“Visionox Dashboard UI”这个 skill 来了解设计风格。
3. 精简 UI：每个选项卡仅保留三部分：需求输入框，结果输入框（或者点击下载的按钮），日志（放入一个 Expander 中并默认折叠）。删除除此之外所有冗余的内容（例如各种提示）。
4. 格式整齐：精简后的 UI 应当看起来格式整齐，三个选项卡风格一致。

--------------------
【Task4】
1. 请在 UI 上执行冒烟测试，完成从需求解析到报表下载的全套流程（测试内容可参考 Task2）。你可以调用 browser-use 或 Playwright 等 MCP 来操控 UI 模拟需求输入。报表下载则调用“Finereport Rpa”。

--------------------
【Task5】
1. 文件名后缀：在下载的报表文件名称后，加上筛选条件，便于智能体迅速理解其中内容。

--------------------
【Task6】
1. 文件下载成功后，请调用文件解密skill“file_decryption”将其输出到如下路径“resources\decrypted_files”，方便后续模块调用。