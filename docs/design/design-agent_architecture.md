【Background】
excel-generator-project是一个日报自动生成Agent。该项目分为三个模块（位于yield_report下）：报表下载、数据分析、日报生成。
当前Agent效果极不理想，因为我思维受限，太多依赖代码，无法适应用户复杂的需求。

【Goal】
你作为一名标准的Agent工程师，帮我设计一个简单使用，但能够持续运转的Agent架构，代替当前项目架构：
1. 经过您的介绍，我深入学习之后总结出的Agent架构如图所示。请分析我的理解是否正确。
2. 请您修正我的架构，并在此之上针对我的项目设计一个Agent架构（如果您觉得我的理解乏善可陈也可以完全推翻）。
3. 最终，请您生成一份能够让Codex执行、详细全面、切实可行的架构重构文件（可供下载的md文件）。

【Context】
1. 我的想法是，直接让Codex作为核心，使用Codex CLI的方式调用Codex。这样一来，我只需要设计UI作为给Codex输入指令的窗口。其余模块可以使用Codex自带的。
2. 请尽量让LLM来帮助我完成任务，而不是代码。只有保留极其简单和重复性高的流程（通常为数据获取）才继续使用Code。
3. 请注意图片中暂不需要的部分，因为我要做的Agent非常简单，并不是一个通用Agent，所以您介绍的大部分高阶功能都不需要。
4. 如果“使用Codex作为核心”的想法不行，您必须思考技术路线，比如我们是否要依赖LangChain来作为智能体框架。尽量使用成熟的架构和项目（比如智能体记忆方案可以使用Github上一个名叫agent-memroy的项目），尽量避免自行研发，因为对于我如此简单的项目不值得投入过高的研发成本。