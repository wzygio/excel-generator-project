【Background】
我要制作一个日报自动生成的项目。本项目分为三个模块：报表下载、数据分析、日报生成。
1. 目前前两个模块已开发完成。请继续帮助我完成“日报生成”模块的开发，并完成相应的任务测试。
2. 三个模块的关系为：日报生成＞数据分析＞报表下载。
a. 报表下载：基础功能，通过从FineReport平台下载报表的方式获取数据源文件。
b. 数据分析：数据分析前，如果发现本地没有文件，则需先调用报表下载模块获取数据文件
c. 日报生成：该模块为一套整合业务流，对应一个spec文件。则需要先从spec文件中提取数据分析逻辑，随后调用模块二进行数据分析，最后将结果按照固定格式整合为报告。

【Introduction】
该模块详细开发方案请参考：“specs\spec-daily_report.md”

--------------

【Task1】
请分析并理解该模块，然后制定开发方案：
1. 我们需要将该模块转化为一个Skill。这样一来，用户只需使用自然语言修改spec文件，即可更改日报生成逻辑。其中关键在于，哪些功能可以沉淀为代码，哪些功能需要有LLM完成。
2. 请审示这份spec，分析有哪些模糊的地方需要补充。

--------------

【Task2】
1. 请在 UI 上执行冒烟测试，完成从“resources\spotfire.xlsx”中提取当日过货产品，到针对这些产品生成日报的全套流程。最后请检查生成的日报是否符合spec文件中的要求（包含四点）：“specs\spec-daily_report.md”
2. 你可以调用 browser-use 或 Playwright 等 MCP 来操控 UI 模拟需求输入。
3. 这一流程注定非常复杂。所以我的建议是，请你将整套流程先拆分为子任务，对应每个层级（日报生成＞数据分析＞报表下载）。并将任务清单输出到“docs\plans”下。
4. 直接调用相应层级的skill进行子任务测试。如果子任务能够正确执行，但是父层级调用时无法得到正确结果，那就是层级之间交互的问题，否则就是对应层级的功能不够完善。

--------------

【Task3】
谢谢。任务基本完成，但还有一些细节需要打磨。
1. 日报生成：如果是上午十点前，那么查询的日度良率时间依旧截止到昨日。
2. 数据分析：当前数据分析功能好像还不能正常运行。请再测试一下。
3. 报表下载：当用户想要查询批次良率时，查询的报表应该是“V3良率及不良率By批次汇总报表”，筛选条件为：“开始时间设定为三个月前（例如今日为2026-06-02，在此基础上减九十天），结束时间设为当天，如果是十点前就是昨日（格式依旧为2026-06-02）。”
4. 报表下载：当用户或上层模块想要查询的内容本地不存在，而Agent又无法判断具体的报表名称时，应该执行以下流程：从用户需求中提取出关键词，调用FineReport RPA这一skill，到帆软报表系统中进行搜索（选择最关键的一个关键词即可，帆软的搜索框目前无法实现模糊查询）
5. 项目启动。请您使用start_test.bat脚本来启动项目，或仿照其中的流程。确保服务永远启动在8502端口，且每次启动前杀死旧进程。

---------------

请分析C516近一周MVI产出占比的变化趋势，请查看表头寻找近一周的日期，一般为最后七列